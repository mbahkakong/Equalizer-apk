<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Izin Efek Audio & Background Service -->
    <uses-permission android.permission.MODIFY_AUDIO_SETTINGS />
    <uses-permission android.permission.FOREGROUND_SERVICE />
    <uses-permission android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK />

    <application
        android.allowBackup="true"
        android.icon="@mipmap/ic_launcher"
        android.label="Universal Equalizer"
        android.supportsRtl="true"
        android.theme="@style/Theme.Material3.DayNight.NoActionBar">

        <activity
            android.name=".MainActivity"
            android.exported="true">
            <intent-filter>
                <action android.name="android.intent.action.MAIN" />
                <category android.category="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Background Service untuk Equalizer -->
        <service
            android.name=".EQService"
            android.enabled="true"
            android.exported="false"
            android.foregroundServiceType="mediaPlayback" />
    </application>

</manifest>
